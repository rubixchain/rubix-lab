# PR #739 - CONFIRMATION SUITE
# Branch: maneesha/fix/denom-array-updation
#
# This is not the full PR suite (that is pr-739.txt). This is the evidence
# package for the review: every finding, and every proof the fixes work,
# in one report short enough to run against TWO builds.
#
# Run it twice - once on the PR branch, once on the merge-base - and diff:
#
#     python3 case_runner.py --suite pr-739-verify --report-name pr739
#     ...deploy the pre-PR build...
#     python3 case_runner.py --suite pr-739-verify --report-name base
#     python3 compare_reports.py <pr739.json> <base.json>
#
# A case that FAILS on the PR build and PASSES on the base was INTRODUCED by
# this PR. That is the whole causation question, and nothing else answers it.
#
# GEN-IN-12/15/22 are genuinely fleet-wide again as of 2026-09-10. They used to
# iterate their own lane's hosts and report "4 host(s) checked" where they
# meant 31 - a clean result they had not earned. CaseContext now carries the
# full ready list as ctx.fleet, and every one of those rows states its scope,
# so "31 host(s) checked (fleet)" can be quoted and "(LANE ONLY)" cannot.


# === PROOF THE THREE FIXES WORK =============================================
# These should be green on the PR build and are expected to FAIL on the base -
# that is what "the fix works" looks like in the comparison.

SC-C-02         # 144cb081: a 0.001 contract costs 0.001, not a whole RBT
SC-C-13         # the 13-value ladder, 0.001 -> 10.375
SC-C-20         # the change token reconciles exactly
SC-C-21         # split children all have chain rows, no orphans
SC-Q-07         # df07a49f: quorum counter correct after it pledges
GEN-IN-18       # df07a49f: one deploy, zero denominations disagree
GEN-IN-16       # 977f6fba: one FT mint, zero denominations disagree
GEN-IN-09       # 977f6fba: no phantom denom=0 row - the exact old signature
GEN-IN-17       # a REJECTED mint moves no counter


# === BLOCKER 1 - rejected deploy loses its collateral ========================
# Three cases, one wallet, this order. SC-C-33 says what SUCCESS does, SC-C-27
# says what REJECTION does, SC-C-31 says whether rejection's version ever
# unwinds. Each one alone is answerable; together they are not.

SC-C-33         # CONTROL: a deploy that succeeds - Committed +value, and a
                # contract to show for it. Must run before 27 empties the wallet
SC-C-27         # ~1136 RBT -> Committed for a deploy that was REJECTED
SC-C-31         # does any of it come back? Committed has no release path
GEN-IN-22       # fleet-wide: committed RBT with no contracts behind it


# === BLOCKER 2 - the receiver credits the collateral =========================
# CRS-C-03 rests on a single reading (+1.206 for 1.0 + 0.206). CRS-C-06 varies
# the collateral so "the excess is the collateral" stops being an inference.
# This one is doubly important: CRS-C-03 fails on BOTH builds for DIFFERENT
# reasons, so compare_reports.py calls it PRE-EXISTING and the bucket lies.

CRS-C-03        # receiver +1.206 for a 1.0 transfer + 0.206 deploy
CRS-C-06        # same bundle at two values - does the excess track v?


# === NON-BLOCKING FINDINGS ==================================================

SC-C-29         # three contracts in one request -> "exceeds 3 decimal places"
SC-C-32         # binary-exact vs binary-inexact: is that float accumulation?
SC-C-34         # the ladder: N=1, then 0.1+0.2, then 0.25+0.5 - where and why
SC-C-11         # zero-value deploy returns a raw 500


# === NOT THIS PR - carried so the report can say so with evidence ===========
# The pledge path and the lock-release paths are untouched by #739. These are
# here to be ATTRIBUTED, not to be fixed by this branch.

SC-Q-12         # quorum drift as a DELTA across one burst, not the stale 0.023
GEN-IN-08       # absolute counter baseline (expect the historical 0.023)
GEN-IN-24       # a rejected FT MINT leaves locks behind
GEN-IN-20       # a rejected TRANSFER does not - the contrast is the finding
GEN-IN-15       # fleet-wide stranded locks
GEN-IN-12       # no negative counts anywhere
